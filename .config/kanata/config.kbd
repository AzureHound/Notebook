(defcfg
  linux-dev /dev/input/event2
  linux-continue-if-no-devs-found true
  process-unmapped-keys yes
)

(defsrc
  caps
)

(deflayer base
  (tap-hold-press 1 200 caps (multi lsft lctl lmet lalt))
)
